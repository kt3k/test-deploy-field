{
  "deploy": {
    "install": "deno install",
    "build": "deno task build",
    "mode": "static",
    "staticDir": "dist",
    "singlePageApp": true
  },
  "tasks": {
    "build": "deno -A build.ts"
  }
}
